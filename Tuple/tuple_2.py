
# Tuples cannot be manipulated
numbers = (1, 2, 3)
print(numbers[0])

# Unpacking tuples
coordinates = (1, 2, 3)
# x = coordinates[0]
# y = coordinates[1]
# z = coordinates[2]

# Same as declared on top
x, y, z = coordinates
print(x)
